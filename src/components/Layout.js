import { Container } from "react-bootstrap";
import Header from "./Header";

const Layout = () => {
    return (
        <Container>
            <Header />
        </Container>
    );
};

export default Layout;